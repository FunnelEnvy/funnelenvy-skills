# Changelog

## [Unreleased]

### Added
- Added an in-output-KB experiment-history fallback: hypothesis-generator KB mode reads a silver `schema: experiment-history` artifact (plus its per-experiment gold insight guides) when external producer-KB discovery binds nothing; backward-compatible, additive. [in-kb-experiment-history-read-path]

### Changed
- Tactical Test Feasibility is now measurement-method-aware. Pre/post (and cohort/operational) engagements use baseline-plus-read-window feasibility and are never demoted or excluded on A/B traffic-volume grounds; A/B engagements keep enforced two-proportion power with honest routing-out. Method is detected from an engagement-context signal, else inferred from experiment-history design_type (majority > 50% non-A/B), else defaults to A/B (no regression). New detect.md Step 1h (Testing-Method Detection), construct.md Step 5b tactical non-A/B branch, method-scoped validate.md Gate 5/6 reads + score.md routing/Impact + SKILL.md Quality Rules 10/16 + read-window render forms, validator block [19]. Documented residual edge (history-less + signal-less pre/post defaults to A/B) filed as follow-up engagement-context-testing-method-signal. [measurement-method-aware-tactical-feasibility]
- Displacement-aware primary read. When bound experiment history establishes a displacement / inverted-proxy mechanism on a surface (an on-page proxy that moves opposite to the downstream business metric), every hypothesis on that surface now reads the downstream metric as primary with the on-page proxy demoted to a leading indicator, and the ship/abandon threshold keys to downstream so an engagement-negative / downstream-positive variant is not wrongly abandoned. Bounded to the observed surface plus strictly same-type surfaces. New detect.md Step 1g displacement read (emitting displacement_surfaces, tri-state/penalty-free), construct.md Step 5 override + Step 4 abandon case + Step 5a guardrail-primary reconciliation for uninstrumented downstream, SKILL.md purity guard + Target-metric render, validator block [20]. [displacement-lesson-primary-read-propagation]

## [1.15.1] - 2026-07-14

### Changed
- Prose-style Quality Rules consolidated to the shared `modules/prose-craft.md` module. Former Quality Rules 8 ("No em dashes") and 9 ("No hedge words") collapse into a single Rule 8 that points at the canonical humanizer / anti-AI-writing reference (em dashes, hedging, and the full sign set); rules 10+ are unchanged, and the two inline fragments are no longer duplicated in the skill. `prose-craft.md` is added to the Phase 1 "Module resolution and availability" hard-require list and the module tree, so a missing module fails loud like `copy-craft.md`. Faithful refactor: nothing previously enforced is dropped, and applying the full sign set is an intended quality improvement. [prose-craft-module-consolidation]

## [1.15.0] - 2026-07-07

### Added
- Tactical `**Change type:**` field (P1 of the experiment-mockup treatment-quality bundle). Every tactical hypothesis now carries a change type derived from its proposed change, from the enum `insert | replace-copy | modify | remove | reorder` (primary first, comma-separated when a bundled test spans types). `phases/construct.md` Step 3 gains the Change-type derivation block (enum definitions + bundled-comma rule); the tactical Output Format renders the field directly after `**What to test:**`; new Quality Rule 19 mandates it. The field is projection content: it re-derives on every render from the proposed change and is NOT carried forward across regens like `**Key:**`. The strategic roadmap is untouched (its levers are not page-element treatments). Consumed by experiment-mockup v1.5.0 to select a type-appropriate treatment. Backward compatible: experiment-mockup classifies the type locally from What-to-test + Proposed-change on roadmaps produced before this field existed; render-program-site and cro-roadmap-red-team tolerate the additive field line. No renumbering of existing rules or steps. [experiment-mockup-treatment-quality]

## [1.14.1] - 2026-07-07

### Fixed
- Unsatisfiable device trigger: the Step 1c "mobile bounce >10pp higher than desktop on same page" trigger could never fire (no producer emits per-page device data; ga4-audit and aa-audit report device splits site-level only). Rewritten to key on the site-level Mobile vs Desktop Gap that profiles actually carry, with a mobile-share floor and `top_pages` targeting; trigger evaluation rules now state the site-level-by-design constraint, and the Profile Schema Equivalence table gains the device-gap equivalence row.
- Trigger-count arithmetic: SKILL.md's additive history ("8 new triggers", "4 additional") implied 19 performance-driven triggers; the Step 1c table has 22. Replaced with a single authoritative statement pointing at the table (count: 22).
- Read-side Mapping basenames: the `silver-competitive-analysis` / `silver-audience-analysis` rows showed the legacy basenames (`competitive-landscape.md` / `audience-messaging.md`) as the funnelenvy default; positioning-framework actually writes the type-derived `competitive-analysis.md` / `audience-analysis.md`. Rows corrected and the example sentence reworded (type-derived = default, legacy-derived = variant). Resolve-by-type rule unchanged.
- Operating Modes accuracy: the KB-mode summary claimed reads resolve "via the gold artifact's `depends_on`" (impossible on a first run; the gold artifact is this skill's output). Now states reads resolve by artifact type per Read-side Mapping, with `depends_on` recording what was consumed; "(current production)" label dropped.
- Profile Schema Equivalence self-gating extended to version-stamped profiles lacking a referenced structure (e.g. an aa-audit profile stamped "2.1" with `element_interactions_available: true` but no `top_interactions[]`): the version stamp gates eligibility, not firing; firing always requires the actual data.
- copy-craft follow-through (module + `phases/construct.md` Step 4b): on-page presence is not verification; a live-page-verified number absent from the L0 proof registry is carried behind a back-fill prerequisite (registered proof point via client confirmation or positioning-update) and is neither silently dropped nor silently shipped (new Step 4b verification step 5, `proof_integrity_passed: false` Confidence cap applies). Rule 12's example CTA de-first-personed ("See pricing plans") with a Rule 15 cross-reference; Rule 4 gains the scope-match requirement (cross-referencing Rule 20); new Rule 23: non-terminal step buttons state the next step, only the terminal button states the outcome. No rule renumbered.

### Added
- Phase 1 hard guard now covers `modules/copy-craft.md` and `modules/slugify.md` (their absence previously degraded silently to remembered rules, exactly what the guard exists to prevent).
- Strategic gold pre-write registration check (KB mode): if a strategic lever qualified and the bound type skill does not define `gold-strategic-roadmap`, the artifact is still written but the completion message leads with the missing-registration note instead of a cold post-write validation failure. The registration-is-a-per-client-follow-on position is unchanged.

## [1.14.0] - 2026-07-05

### Added
- `modules/copy-craft.md` (v1.0.0): evidence-graded copywriting rules for proposed variant copy (headline/H1, hero subhead, CTA button copy, CTA microcopy, form labels and error/privacy microcopy). Twenty-two rules grouped by element plus cross-cutting rules, each carrying a strength tag (strong / moderate / heuristic) and a finding citation; an evidence-quality preamble stating the B2B evidence gap honestly (the strong ground is comprehension/usability mechanism evidence, mostly not B2B-conversion-specific); a verbatim do-not-encode folklore list; and a tiered source appendix. Encodes direction and mechanism, never folklore lift percentages. The first-person-CTA "law" is kept only as an explicitly de-rated test idea (Rule 15).
- Wired the module into `phases/construct.md` at five anchors: Required Inputs (copy-craft listed for copy-bearing hypotheses); Step 3 before/after (proposed copy across all copy-bearing categories written against copy-craft, specificity deferring to Step 4b); Step 3b scope note (copy-craft compliance orthogonal to the strategic-anchor requirement); Step 3b variation `copy` block (each field written per copy-craft); and Step 4b (reciprocal cross-reference: copy-craft Rules 4/20 route verification through the proof-integrity check). Craft-level only: the Experiment Scope Rule, bundling, ICE scoring, and the strategic lane are unchanged. CTA-label and form-microcopy hypotheses, previously ungoverned for copy, now receive craft-checked copy via Step 3. [hypothesis-generator-copy-craft-module]

## [1.13.3] - 2026-07-05

### Fixed
- Repo-audit doc corrections, no behavior change: the Module Dependencies tree now marks `modules/patterns-b2b-saas.md` as planned/not-yet-on-disk (the resolver-skips-if-absent caveat previously lived only in the Phase 1 table, so the tree advertised a module that does not exist); the contrarian trigger count corrected from 13 to 14 in the tree annotation and in `phases/construct.md` (the module defines CTR-01 through CTR-14; CTR-14 was added in 1.6.0 but two counts were never updated). Content-only change to `SKILL.md` and `phases/construct.md`. Also gains the `modules/kb-mode.md` canonical-contract pointer in its KB-mode section (drift canary enforced by `scripts/registry_check.py`). Quality Rules moved after Module Dependencies so the file ends with the quality checklist (pure section reorder, no wording changes).

## [1.13.2] - 2026-07-04

### Fixed
- Five text-determinacy clarifiers accumulated from a 1.13.1 production regeneration against the richest input set exercised so far (structural observation, a lead-lifecycle measurement reference, an experiment-history producer KB, and a second performance profile). Each run resolved these defensibly at the time; the text now decides: (1) `detect-strategic.md` criterion 7 names a fourth state, documented-in-flight -- a client initiative documented as decided and in Design/Build discards via criterion 5 (genuine uncertainty is already settled), notes in "What's Not Here" as an in-flight initiative (distinct from an existing-capability note), and the residual rollout-read experiment may be minted on its own merits once the capability ships; (2) criterion 7's context-is-silent state extends to a structural-observation field recorded `not_checked` for a website surface (a page or asset the capture did not check), symmetric with client-side-system silence -- both are something the loaded context could have borne on but does not, as distinct from documented-absent's comprehensive-inventory silence; Required Inputs gains the structural observation artifact as a named (optional) source; (3) the strategic `depends_on` frontmatter contract reworded from an enumerated list of four silver types to "including but not limited to," so load-bearing scope reference docs (a lead-lifecycle/measurement reference, engagement context) and structural observation that are genuinely consumed are not left off `depends_on` merely because the sentence never named them; (4) `score.md` Step 6b states explicitly that a Cut-tier strategic survivor (measurable, scored, ICE < 7) is dropped silently, exactly as a tactical Cut is, and is NOT listed in the strategic "What's Not Here" (reserved for not-measurable / existing-capability / decision-blocked items, never for a scored-but-low lever); (5) `SKILL.md` Read-side Mapping's scope-isolation rule gains one narrow read-only exception: a consumed in-scope artifact's own declared `depends_on` edge into a cross-scope reference artifact (e.g., a shared persona taxonomy backing an audience-messaging silver artifact) is readable transitively through that edge, without loosening the absolute rule for any undeclared cross-scope read and without adding a new cross-scope edge to this skill's own output. Content-only change to `phases/detect-strategic.md`, `phases/score.md`, and `SKILL.md`. [hypothesis-generator-strategic-lane-clarifiers-2]

RELEASE GATE (1.13.0) RESULT: PASSED 2026-07-02 in a consumer repo (restated; this patch carries no new gate of its own, per the 1.11.1/1.12.1/1.13.1 fixed-only-patch precedent). See the 1.13.0 entry below for the original three assertions.

## [1.13.1] - 2026-07-02

### Fixed
- Three text pins from the 1.13.0 release-gate regression (all three gate assertions PASSED; these close low-severity determinacy gaps the run flagged): (1) the R6 case-2 pair predicate is now the explicit conjunction of same underlying idea AND shared read surface, with a multiple-pair rule (the strategic line names each qualifying tactical test; each qualifying test carries its own converse); (2) a regeneration-only strategic run never writes the tactical deliverable: its side of a case-2 line (and any case-1 drop) lands on the tactical roadmap's next re-render, with the pending tactical-side annotation noted in the completion message; (3) the tactical template's "no strategic-lane lines" sentence is scoped to fields and per-experiment content, naming the case-2 Sequencing Rationale scheduling line as the one permitted cross-reference (removing a literal reading that would suppress it). Content-only change to `phases/detect-strategic.md` and `SKILL.md`. [hypothesis-generator-strategic-lane-text-clarifiers]

RELEASE GATE (1.13.0) RESULT: PASSED 2026-07-02 in a consumer repo. Assertion 1: detection replay produced zero survivor-set or family-routing drift vs the 1.12.1 baseline (same 3 bets, same 2 Foundation entries, same discards), and the R6 scheduling lines rendered register-clean on the one case-2 pair. Assertion 2: the pre-flight elicitation question correctly did not fire with the engagement-context soft input present, fires at most once on an interactive no-soft-input run, and never penalizes non-interactive or unanswered runs. Assertion 3: the archetype family extension is inert with no archetype strategic families defined, and the purity list covers archetype-defined tokens.

## [1.13.0] - 2026-07-02

### Added
- Active soft-input elicitation (R5): the Phase 2c optional soft input (stated business objective, dominant lever, real alternative) stays tri-state and penalty-free, with two additions on top. (a) The KB-mode mapping gains an explicit first-class source: an engagement-context / engagement-constraints reference artifact in the bound KB, when one exists, IS the soft input and MUST be read (strategy-context artifacts that reference such a document for lever status make it load-bearing; skipping it reproduces the already-solved-gap defect class). The Read-side Mapping gains a matching optional row. (b) On an INTERACTIVE run with no soft input present in any loaded source, the phase asks ONE consolidated pre-flight question before Phase 2c detection covering the three fields; asked at most once per run, never blocks a non-interactive run, and an unanswered question proceeds exactly as today, penalty-free. Family 1 additionally resolves inferred-objective sufficiency: an inferred objective (strategy context plus the performance profile's own conversion definitions) suffices for the mismatch read when those conversion definitions make the mismatch observable without the inference doing the work; the elicited soft input is preferred when present. [hypothesis-generator-strategic-lane-text-clarifiers]
- Cross-deliverable interaction note (R6): Step 3 dedup case 2 (same idea, different measurement altitude, both stand) now annotates both records with a scheduling constraint, rendered as ONE natural-language line in each deliverable: the strategic experiment's "Read condition and window" line states the page-level test on the shared surface must not run overlapping the strategic read window (or must be accounted for in the read), and the tactical Sequencing Rationale states the converse. Register rules apply in full: neutral, connective, never evaluative, no internal tokens. Both SKILL.md template brackets extended by one clause each.
- Archetype strategic-family extension point (R7), shipped INERT: mirroring the tactical archetype layer, a new "Archetype Family Extension" subsection in detect-strategic.md (after the six families, before the Catch-All Leg) states that when Phase 1's archetype resolver loaded an archetype pattern module defining a `## Strategic Lever Families` section, those families load additively as families 7+ for the run: same gate, same record shape, `lever_family` takes the module-defined token, and archetype families count for family precedence (checked before the catch-all). Inert today: no archetype module defines strategic families; a module without the section contributes nothing, penalty-free. Deliverable Purity extends the banned lever-family enum-token list with any archetype-defined lever-family token.

### Fixed
- Ten strategic-lane text-determinacy clarifiers accumulated from the 1.11.0 release-gate regression and two production regenerations (each run resolved these with a defensible judgment call; the text now decides): (1) SKILL.md Re-render Behavior states that a prior scored strategic experiment demoted to a Measurement Foundation entry on regeneration orphans its `**Key:**` by design (Foundation entries are keyless) and is reported as a demotion, not key churn; (2) validate.md Strategic Gate 2 codifies the primary-metric framing preference: a bet honestly framable on a readable-today primary metric (live-elsewhere pass) with the Foundation-gated qualified layer as enrichment prefers that framing, reserving the Foundation-dependent fail for bets whose thesis is only about the gated metric; (3) the supersede version rule (both the tactical and strategic statements) counts a change in the SET of consumed silver artifacts as "changed": a newly consumed input triggers the minor bump; (4) family 1 inferred-objective sufficiency, folded into R5 above; (5) blocked-pending-decision routing named in the quality gate: a lever passing all seven criteria but documented as blocked pending an explicit client decision routes to the strategic "What's Not Here" as a decision-blocked item carrying its demand evidence and the unblocking decision, re-evaluated next run, never minted while blocked (matching allowance added to the SKILL.md strategic What's Not Here bracket); (6) ice-scoring.md gains a strategic Ease clause parallel to the strategic Impact/Confidence clauses: anchor Ease on stand-up scope (4-5 content/config-level, 3 cross-team operational definition, 2 new-capability build on contended engineering capacity, 1 business-model/compliance/channel change); strategic deliverable only, tactical Ease unchanged; (7) score.md Step 6b and the ice-scoring portfolio rules state the spread rules (Impact spread, at-least-one-low, forced-spread) are tactical-portfolio rules that apply proportionally on the strategic pass and never force artificial variance at three or fewer experiments; (8) construct.md Step 4c blesses the two-stage read pattern: finalize the single `measurement_design` token for the readable-today primary leg and describe the second leg in prose as a Foundation-gated enrichment, never a second token; (9) the strategic How to Read tier sentence rewords Explorations as higher-uncertainty or lower-scoring bets carried for their learning value, covering Impact-gated placements; (10) detect-strategic.md Step 3 states that a regeneration-only strategic run performs the cross-deliverable reconciliation against the on-disk tactical roadmap, read after detection completes. [hypothesis-generator-strategic-lane-text-clarifiers]

RELEASE GATE: bound-KB regression in a consumer repo before release: (1) a family-covered scope's detection output is unchanged vs 1.12.1 except the R6 scheduling line where a case-2 dedup pair exists; (2) the elicitation question fires only on interactive runs with no soft input present; (3) archetype extension is byte-inert with no archetype strategic families defined.

## [1.12.1] - 2026-07-02

### Fixed
- Seven text-determinacy route-backs from the 1.12.0 release-gate regression (both legs PASSED all four gate assertions; these pin judgment calls the runs resolved defensibly but the text left open): (1) two-artifact independence defined at the evidence-origin level (an artifact echoing its `depends_on` parent's claim, or two artifacts restating one underlying capture/figure, is ONE signal); (2) the second signal need not re-name the lever (lever named once + independently derived value evidence meets the bar, per the leg's own example, now codified); (3) channel-scale programs (SEO, generative/AI-search optimization, organic content, paid-media strategy) added to the catch-all anti-patterns: they fail criterion 2 by construction since their intervention unit needs channel analysis outside the skill's inputs; real demand routes to "What's Not Here" as a channel decision (kills the observed run-to-run nondeterminism risk on AI-search-shaped candidates); (4) family precedence matches on a family's DEFINITION sentence, not its name, and a near-miss family never consumes-and-discards (this boundary is score-consequential: it decides whether the catch-all -1 applies); (5) catch-all measurement-shaped finds route to the Measurement Foundation slot exactly as family 1a stand-ups do (unscored, never Impact-scored); (6) criterion 7 names the built-but-disabled middle state: implemented-but-disabled/unentitled/subset-scoped is documented-live with a documented-open enablement gap, minting only the enablement/adoption shape (refinement work is that experiment's stand-up dependency, not grounds to reframe as a build); (7) graceful-degradation row reworded per-candidate, and `signal_source` for catch-all records specified as a list. Content-only change to `phases/detect-strategic.md`. [hypothesis-generator-strategic-catchall-leg]

RELEASE GATE (1.12.0) RESULT: PASSED 2026-07-02 across two consumer-repo legs. Recall: the catch-all leg minted the intended out-of-family dormant-capability lever from two independently derived artifact signals, with the extra -1 visible and consequential in the scoring arithmetic (raw 4 -> de-novo 3 -> catch-all 2; ICE 7 Exploration), and correctly applied criterion 7's documented-live reading to shape it as enablement, not build. Precision: on a fully family-covered scope the leg considered three net-new candidates, killed all three at the gate, zero catch-all survivors, deliverable set identical to 1.11.1. No family cannibalization on either scope (every prior survivor re-minted via its same family; the leg added only). Purity held.

## [1.12.0] - 2026-07-02

### Added
- Catch-All Leg in Phase 2c (`phases/detect-strategic.md`): after the six lever families, a licensed family-agnostic full-body read for business-level levers the context affirmatively names that fit no family. The six families stop being the recall boundary (field evidence: the lane's best-grounded production bet fit no family and was found only by an unlicensed generous read; channel/motion-conflict, merchandising-program, lifecycle-reactivation, and pricing-policy levers had no family home). Design: raised two-artifact signal bar (a catch-all candidate must derive from signals in two independent loaded artifacts; the bar replaces the family template's structural constraint), the identical 7-criterion quality gate verbatim (incl. criterion 7 tri-state), explicit zero-survivor expectation (mirroring Phase 2b's framing), named anti-patterns, and family precedence (a candidate matching a family routes through the family; the leg only adds, never re-routes). Survivors mint `lever_family: catch-all` (token added to the Deliverable Purity banned enum) with the standard 3/3/3 baseline and take one additional -1 raw-Confidence soft modifier beyond the de-novo -1 (stacking with the confirm-first contingency when both apply; soft modifiers, never a ceiling - a hard cap would make a well-evidenced catch-all lever permanently second-class, and the 1.11.x Strategic Lane Gates already provide hard enforcement). Identical construction, gate, scoring, and render path; rendered output indistinguishable from family levers. Graceful degradation: under two bearing artifacts, the leg emits nothing penalty-free. Byte-inert when it finds nothing. Sequenced deliberately after the 1.11.x validation lane existed to receive its output. [hypothesis-generator-strategic-catchall-leg]

RELEASE GATE: bound-KB regression in a consumer repo before release, with four named assertions: (1) recall - on a scope whose context names an out-of-family business lever with two-artifact support, the leg mints it, gated, with the extra -1 visible in the scoring arithmetic; (2) precision - on a scope with no such lever, zero catch-all survivors and output byte-identical to 1.11.1; (3) no family cannibalization - every lever a 1.11.1 run minted via a family still mints via the same family; (4) purity - the `catch-all` token absent from rendered bodies.

## [1.11.1] - 2026-07-02

### Fixed
- Five defects surfaced by the 1.11.0 release-gate regression (bound-KB dossier-free strategic-lane run in a consumer repo, plus a counterfactual/inertness pass; all three named gate assertions PASSED, these are the route-backs): (1) the ice-scoring Impact-anchor guard is lane-scoped to the strategic pass; its prior "(or any Impact anchor)" phrasing contradicted the tactical lane's v1.6.0 variant-instrumented measurement-enablement prerequisite-experiment, which remains a scored tactical object; (2) a Foundation-entry-only strategic output now has a rendering path: Step 5c and the Output Format produce-condition fire on "at least one scored strategic experiment OR at least one Measurement Foundation entry" (family 1 already declared Foundation-only output complete and correct, but the old Step 5c condition silently dropped it); (3) Strategic Gate 1 record semantics after re-finalization: the gate evaluates the FINAL design, reads pass/not-assessed when construct.md Step 4c already moved the design off the disqualified baseline, and reserves fail for a final design that still consumes it (the no-alternative case); (4) the strategic "What's Not Here" spec now explicitly holds the existing-capability notes that criterion 7's documented-live routing sends there, resolving a conflict with its measurable-by-no-design framing; (5) criterion 7 boundary clarifier: comprehensive-inventory silence on a surface the context DOES cover (the site's offer set, its event inventory) reads as documented absence, not context-silence; the confirm-first state is reserved for systems the context was never positioned to observe. Content-only. [hypothesis-generator-strategic-rigor]

RELEASE GATE (1.11.0) RESULT: PASSED 2026-07-02 in a consumer repo. Assertion 1 (baseline re-finalization): both surviving levers' candidate designs (pre/post, cohort) were re-finalized away from the documented-unreliable baseline (forward-only windows; randomized concurrent control). Assertion 2 (Measurement Foundation): the qualified-lead instrumentation emerged as an unscored Foundation entry, never a scored bet. Assertion 3 (confirm-first): the client-side-system-silence case rendered with the existence check as step one in neutral register. Byte-inertness: all 1.11.0 additions verified inert on a no-lever run (the one conditional leak found is fix 1 above). Deliverable purity held.

## [1.11.0] - 2026-07-02

### Added
- Strategic-lane validation gates (Phase 3.5 extension, `phases/validate.md` > Strategic Lane Gates): `baseline_reliable` (an affirmatively disqualified comparison baseline triggers a design-forcing re-finalization in construct.md Step 4c toward forward-only/randomized designs, and caps Confidence at 2 only when no alternative design exists), strategic `metric_instrumented` (tactical Gate 2 logic incl. live-elsewhere posture, with a Foundation-dependent read-today extension), and strategic `premise_contradicted` (unreconciled quantitative contradictions the bet's scoping depends on are an affirmative fail, not a risk footnote). score.md Step 6b consumes the record as a hard Confidence ceiling after all soft modifiers, identical tri-state semantics to the tactical Step 4d. Stand-up depth is codified as dependency-count ordering within strategic tiers (a sequencing rule, not a score gate; no Ease double-count). Placement rationale: the gates extend Phase 3.5 rather than living inside score.md Step 6b, because validation inside the scoring step is the soft-consideration failure this change closes. Closes the enforcement gap where the strategic lane, running the weaker (non-randomized, confound-prone) designs, had the weaker enforcement, and where disclosures the deliverable already wrote (directional-only baselines, unbuilt attribution, unreconciled data contradictions) carried no score consequence. [hypothesis-generator-strategic-rigor]
- Measurement Foundation section in the strategic deliverable (`SKILL.md` Strategic Roadmap Output Format): family 1 (objective mismatch) now emits two distinct object types, an unscored Measurement Foundation prerequisite entry (what gets instrumented, which system, which experiments depend on it; no ICE scores, actionable by the client's analytics team standalone) and, only when the context names one, a scored intervention measured against the newly instrumented metric. The family 1 canonical example that conflated the prerequisite with the experiment in one clause is deleted. Impact anchors gain an explicit guard: instrumentation stand-ups are never scored on any Impact anchor (value-of-information is not a conversion effect). Section, not a new deliverable or artifact type: zero registration or governance surface added. Follows the tactical measurement-enablement precedent. [hypothesis-generator-strategic-rigor]

### Fixed
- Criterion 7 (Gap is still open) is now tri-state: documented-live (discard, unchanged), documented-absent (mint as build), and context-silent-about-a-client-side-system (mint ONLY in confirm-first shape: the first sequenced step is the existence check, `confirm_first: true`, Confidence carries a -1 contingency lifted when verification lands). Silence about a CRM/sales-ops/martech system the loaded context was never positioned to observe is no longer treated as evidence of absence. This is the third face of the already-solved-gap defect class (1.10.1 fixed "context says live, skill says build"; 1.10.2 fixed the tactical analog; this fixes "context says nothing, skill says build") closed before it recurs, rather than after. Confirm-first is the only shape for silence-based levers; a routed standalone client question is not an alternative, because unminted questions do not survive into the deliverable. [hypothesis-generator-strategic-rigor]

RELEASE GATE: bound-KB functional regression must pass in a consumer repo before release, with three named assertions: (1) a pre/post bet against a documented-unreliable baseline is re-finalized to a forward-only or randomized design or capped at Confidence 2 with a directional-only read condition; (2) an objective-mismatch context emits a Measurement Foundation entry and never a scored instrumentation bet; (3) a silence-based lever renders in confirm-first shape with the check as step one. A no-strategic-lever run must remain byte-identical to 1.10.2 output (byte-inertness regression).

## [1.10.2] - 2026-06-29

### Fixed
- Tactical instrumentation path no longer tells the client to "stand up" tracking that is already live in production. This is the tactical analog of the 1.10.1 `detect-strategic.md` criterion-7 / objective-mismatch fix: same root principle (assess "is this capability already live?" by reading *current status* across ALL loaded artifacts, not by defaulting an absent-from-the-performance-profile metric to dark), now applied to the `detect.md` -> `validate.md` -> render pipeline. Three coordinated content changes: (1) `phases/detect.md` Step 1f gains a **live-capability leg** -- a metric documented live / in production / already reporting in any loaded silver artifact (lead-lifecycle / attribution / measurement-reference, strategy/audience context, structural observation), not only `performance-profile.md`, enters `instrumented_metrics` tagged `live-elsewhere` instead of falling to the conservative dark default; (2) `phases/validate.md` Gate 2 (`metric_instrumented`) checks for a live-capability statement before defaulting an absent metric to dark, so a `live-elsewhere` metric `pass`es with a per-surface **confirmation** note rather than an instrumentation-build prerequisite, and only a metric documented live nowhere `fail`s to Prerequisites; (3) the SKILL.md `Readiness` output field and Deliverable Purity mapping render the live-elsewhere case as verification ("the [capability] is live; confirm it fires on this surface before launch"), never as tracking to stand up. No regression: a genuinely-dark metric still routes to Prerequisites with a Confidence cap, and a run with no performance profile still reads `metric_instrumented: not-assessed` (the no-performance-profile precedence comes first in Gate 2). Surfaced on a consumer-engagement run, where a tactical experiment framed a live per-arm qualified-lead attribution (already live in production and documented in a loaded silver artifact) as "not yet instrumented; standing it up is a launch prerequisite." Content-only change to `phases/detect.md`, `phases/validate.md`, and `SKILL.md`; no module-library or scoring-phase change.

## [1.10.1] - 2026-06-26

### Fixed
- Phase 2c strategic detector no longer mints already-closed gaps as experiments. The Quality Gate (`phases/detect-strategic.md`) gains a 7th criterion, **Gap is still open**: a lever whose named intervention (capability, instrumentation, routing, asset, or measurement) is documented as already implemented, live, or operating in the loaded context is discarded and routed to "What's Not Here" as an existing capability rather than minted. Criterion 7 directs the before-state read (criterion 4) at *current status*, not just current shape, so a documented "live"/"in production"/"already reporting" status fails the gate; the only residual experiment a closed gap supports is a usage/adoption test of the existing capability, minted on its own merits, never as "stand up" the capability. The objective-mismatch lever family (item 1) gains a matching precondition: confirm the qualified-lead/down-funnel event is not already instrumented before minting, and route to "What's Not Here" when a performance/measurement/attribution artifact documents per-arm down-funnel measurement is already live. Surfaced on a KB-mode run that minted a top-ranked "measure interventions against qualified-lead rate" strategic experiment whose attribution mechanism was already live in production and documented in the loaded silver context, inverting the Confidence signal (the capability's existence was read as evidence the experiment was sound). The criterion-7 fix is generic and covers the same already-solved-gap defect class across all six lever families. Content-only change to `phases/detect-strategic.md`; no module-library or scoring-phase change.
## [1.10.0] - 2026-06-24

### removed
- Retired the `--present` chaining flag (Invocation table row + Re-render Behavior chaining note removed). It invoked the now-deleted roadmap-presentation skill; auto-chaining is unsound for its replacement render-program-site, which requires two edge-contract-schema inputs a freshly-written roadmap does not yet carry. Re-chaining is deferred to chg_2026-06-24_hypothesis-generator-edge-contract-schema-emission. (chg_2026-06-24_render-program-site-skill) [render-program-site-skill]

## [1.9.0] - 2026-06-24

### Added
- Premise and measurement rigor made binding (tranche 2 of chg_2026-06-09_hypothesis-generator-premise-rigor). New phase `phases/validate.md` (Phase 3.5) runs between construct and score and emits a per-hypothesis `validation_gates` record with six tri-state gates (premise_contradicted, metric_instrumented, baseline_exists, control_stable, powerable, segmentation_satisfied; pass / fail / not-assessed, only an affirmative fail caps Confidence, not-assessed is neutral and never penalizes). validate.md owns premise triangulation (FM1, authority order measured performance > observed structure > inferred positioning), control-stability (FM2, deferring to CTR-14 when mobile_render_clean:false is the only signal), instrumentation enforcement (FM4), and feasibility/power enforcement (FM3). detect.md gains Step 1f (measurement inventory instrumented_metrics/dark_metrics, staleness signals via capture-date and cross-artifact element-disagreement, and the FM9 bot/synthetic contamination flag), each leg skipping penalty-free when its artifact is absent. construct.md adds Step 5 segmentation pre-registration (FM6, Direct>75% / bounce>60% / new>80% dilution thresholds plus a viewport/scroll-reach flag), extends Step 5b (Test Feasibility) power-check to the guardrail metric, the segmented denominator, and an explicit MDE-or-unpowerable determination (FM3), strengthens Step 5c with a bundle-interpretability check (FM7, thesis element must be dominant and seen by most visitors), and makes Step 10 self-critique consequential (FM8, an unmitigated thesis counter reduces Confidence, an unmitigated outcome counter adds a guardrail or reduces Impact, emitted as score_effect). score.md rewrites Step 4 into the gated Confidence rubric (FM5, hard ceiling AFTER the soft modifiers: one affirmative fail caps Confidence at 3, multiple at 2; the former standalone proof-integrity cap-at-3 folded in as one gate condition so the two no longer double-cap), discounts FM9-contaminated traffic/bounce before the Step 4 Impact modifiers, extends Step 5b (Infeasible Routing) to route unpowerable-thesis-metric and dark-primary-metric hypotheses to "What's Not Here," requires all gates pass for Quick Win eligibility (Step 6), and adds the FM10 forced-spread remedy to the Step 5 score-clustering anti-pattern. SKILL.md registers Phase 3.5 in the Execution Pipeline and Module Dependencies, adds a natural-language per-hypothesis Readiness line to the Output Format, reinforces the measurement-reality framing in the How to Read preamble, and extends the Deliverable Purity Constraint with the new internal field names. ice-scoring.md adds a base-rate anchor (most A/B effects are true nulls; do not anchor on vendor case-study lifts), an evidence-source-quality note, a gated-Confidence anchor, and the FM6 dilution thresholds as tunable one-engagement heuristics. Validator: phase count 5 to 6, new phases/validate.md presence check, new block [15] (gate-record marker, six-gate presence, tri-state statements, detect Step 1f header, score.md Step 4d marker, purity-guard checks for the new field names). All checks KB-internal; no new research; legacy / fewer-artifact mode degrades gracefully with no gate-driven Confidence penalty. RELEASE GATE: bound-KB functional regression must pass in a consumer repo before release. [hypothesis-generator-premise-rigor]
- Strategic experiments as a separate, first-class deliverable. A strategic-lever detection phase (Phase 2c, phases/detect-strategic.md) reads strategy/objective/audience/competitive context for business-level levers via an inline six-family checklist (objective mismatch, stakeholder-named off-page lever, positioning/proof gap, real-alternative/status-quo, buying-group motion, offer architecture) and mints first-class strategic opportunities carrying a measurement design, through a relaxed quality gate (page-and-element criterion relaxed to a named intervention unit; falsifiable-mechanism and documented-before-state kept). These flow through a separate construction/scoring/render path into their own deliverable, NOT into the tactical roadmap's tiers: legacy `.claude/deliverables/strategic-roadmap.md`, KB mode `{kb_root}/deliverables/{scope}-strategic-roadmap.md` as a new distinct gold artifact type `gold-strategic-roadmap` (emitted with correct frontmatter; client-KB-type registration is a per-consumer follow-on, out of scope here). The tactical experiment-roadmap and its pipeline are unchanged. Relaxed unit allowance across Quality Rule 2, detect-contextual.md criterion 2, and the construct.md Experiment Scope Rule (program/offer/audience-motion/asset, not only a page element; bundling discipline preserved per lane). Measurement model beyond on-page A/B: construct.md Step 4c selects an explicit measurement design (randomized A/B, holdout, pre/post, cohort, geo split, switchback, operational tracking) with read window and honesty limits; the non-A/B feasibility branch judges a strategic design on its own terms and routes a stand-up-able lever to a strategic tier, never to "What's Not Here" for failing an A/B formula. Strategic scoring is separate from tactical: business-outcome Impact framing and measurement-rigor Confidence apply to the strategic deliverable (modules/ice-scoring.md 1.2.0), while the tactical Impact anchors are untouched by construction (the two no longer share an ICE table). Collision reconciliation: the untestable-differentiator routing (detect-contextual.md criterion 5 / score.md) is scoped to page-element altitude so a business-level lever is the strategic pass's responsibility and is not exiled to the tactical "What's Not Here"; cross-deliverable dedup is same-idea+same-altitude supersede / different-altitude both-stand. New Client-Facing Register output rule (sibling to Deliverable Purity) governs the standalone strategic deliverable, with a prohibited-language list; Quality Rule 17 self-critique renders as "Key risk" in the strategic deliverable. Default-on and context-gated: when no business-level lever qualifies, no strategic deliverable is produced and the tactical roadmap is byte-identical to today. Validator phase count 4 to 5 plus separate-deliverable presence checks. Architecture note: an initial in-tier implementation (strategic experiments merged into the tactical roadmap's tiers) was built and reversed after QA showed it non-deterministic; the separate deliverable removes the routing collision that caused it. RELEASE GATE: bound-KB functional regression in a consumer repo, where clean from-scratch runs confirmed a consistent separate strategic deliverable with no leakage into the tactical roadmap. [hypothesis-generator-strategic-hypothesis-track]

## [1.8.0] - 2026-06-21

### Fixed
- KB-mode Read-side Mapping: corrected the competitive, audience, and performance silver-input paths to use the canonical producer basenames (`competitive-landscape.md`, `audience-messaging.md`, `performance-profile.md`) instead of the `-analysis.md` artifact-type stems, which were written by no producer and read by no other skill. On a KB-mode regen these three optional silver inputs were silently not found, degrading the roadmap and capping all Confidence scores at 4. Added a table-preamble rule: KB mode changes the directory, never the basename. (chg_2026-06-19_hypothesis-generator-kb-silver-input-basenames)
- Experiment-history producer-KB binding now actually resolves. The v1.7.0 experiment-history consumption feature described its input as a `gold-experiment-roadmap` at the skill's own output path (the artifact it WRITES) and provided no way to discover the producer KB, so a bound completed-experiment KB was never read: no completed-experiment evidence, no Step 1g triggers, no Step 4 replication modifier, no Step 7 winner-replication sequencing (all silent, since absence is penalty-free). Corrected the Read-side Mapping cell to the producer's `gold-experiment-index` at the producer KB root, and added Mode Resolution Procedure step 5: enumerate declared KBs and bind the non-output KB whose type defines `gold-experiment-index` (keyed exactly as the output KB is keyed on `gold-experiment-roadmap`; no hardcoded KB type or path). Optional and penalty-free: a missing or broken producer never flips mode or hard-stops. Scope mapping: a producer with a disjoint scope vocabulary (a dedicated experiments KB whose program is the consuming scope's experiment history, with records addressed by experiment id rather than the strategy scope) is read whole instead of filtered to an empty set; literal `--scope` filtering applies only when records carry the run scope's vocabulary. Validator [13] updated. (chg_2026-06-19_hypothesis-generator-experiment-history-producer-binding)

### Added
- All-outcome experiment-history iteration generation (KB mode): detect.md Step 1g now mints first-class iteration hypotheses from the producer KB's per-record next_experiments backlog across all outcomes (winner/loser/flat) and all next-experiment types (rollout/iteration/follow_up/discriminating_test), not just winner rollouts; provenance-tagged, ICE baseline 3/3/3 (no inflation), with a same-mechanism merge test (same surface plus same change = merge; same surface plus different change-under-test = separate experiment) replacing the prior merge-not-emit boundary. score.md Step 4 gains an outcome-aware scoring posture (winner-rollout +1/+1 remains the only replication-grade Quick Win path; discriminating tests +1 Confidence; loss/flat iterations score on learning value; gate ceilings and the soft-modifier-then-gated-rubric order preserved), Step 6 adds a --max within-tier retention rule (drop medium continuations before recommended_lead/high), Step 7 Layer 4 generalizes to Experiment-History Continuation Priority. New Experiment Program Continuity Output Format section names which prior test each new experiment advances. Deliverable Purity Constraint clarified: quantified prior-experiment results (lift %, significance, sample sizes) are permitted as natural-language attribution; the banned confidence scores are the skill's own ICE sub-scores, not measured statistical confidence; prohibited-internal vocabulary extended. Inert and byte-equivalent when no producer KB is bound (tri-state safety preserved). Validator [13] extended; counts unchanged. RELEASE GATE: bound-KB functional regression must pass in a consumer repo before release. [hypothesis-generator-experiment-history-iteration-generation]

## [1.7.0] - 2026-06-18

### Added
- Stable per-experiment `**Key:**` field in the roadmap render: emitted as the first bold field under each `### N. {Title}` heading, minted once via `slugify(title)` and immutable (position-independent, never re-derived from a changed title). New key-minting authoring rule and Quality Rule 18. Regen carry-forward in both modes (legacy `Re-render Behavior` and KB-mode `Prior Work Detection`): before overwrite, build a `prior normalized-title -> **Key:**` map and reuse a prior key on a normalized-title match, mint fresh otherwise; only the key value is carried forward, the rest of the body stays a complete fresh projection. Key-churn reporting in both completion messages (re-minted keys and orphaned prior keys; first-run/no-churn stated explicitly; informational, not a gate). Decouples downstream mockup resolution (experiment-mockup, roadmap-presentation) from mutable heading titles. (chg_2026-06-18_stable-mockup-resolution-key) [stable-mockup-resolution-key]
- Thin `--present` chaining flag: after a successful roadmap write, invokes the separate `roadmap-presentation` skill to render the roadmap as a client-facing multi-page HTML site, passing the same mode (KB `--scope` or legacy). Pure chaining affordance; adds no analytical behavior and changes no scoring, pattern, or output-content logic. Fires in both modes. (chg_2026-06-10_experiment-roadmap-presentation)
- Optional experiment-history consumption (KB mode): when the bound repo declares a completed-experiment knowledge base, read its gold index plus the linked silver insights (read-only, scope-filtered with cross-scope allowed, no depends_on edge), surface a new Evidence From Completed Experiments section in the roadmap, activate the Step 7 winner-replication sequencing reorder, and add a Step 4 replication Confidence/Ease modifier (raw-Confidence lift; a gated Confidence rubric stays a hard ceiling); detect.md Step 1g experiment-history triggers; absent input never penalizes; Deliverable Purity and validator additions. (chg_2026-06-14_hypothesis-generator-experiment-history-consumption) [hypothesis-generator-experiment-history-consumption]

## [1.6.0] - 2026-06-11

### Added
- Dual-mode I/O (KB mode): reads the scope's silver CRO artifacts from a bound knowledge base instead of .claude/context/ and writes a typed gold-experiment-roadmap artifact; --scope and --no-kb flags; mode resolution with loud legacy fallback; gold frontmatter contract with gold-to-silver depends_on; supersede-in-place re-runs; post-write validation gate; Profile Schema Equivalence rule in detect.md Step 1c (content-equivalence trigger evaluation for performance profiles lacking schema_version); legacy .claude/context/ behavior unchanged (chg_2026-06-03_hypothesis-generator-kb-native)
- Archetype architecture layer (resolver + loader): Phase 1 resolves an archetype from category.primary via a case-insensitive substring mapping (procurement, b2b-saas, b2c-ecommerce, or base-only on no match) and loads the matched archetype pattern module additively on top of the base library (modules/experiment-patterns.md). detect.md Step 2 gains an archetype-precedence matching rule (archetype patterns checked first, base patterns still fire on an uncovered page + mechanism, ties resolve to the archetype baseline via the existing Phase 3 Step 8 dedup). The pre-flight summary reports the resolved archetype and loaded modules, and Module Dependencies lists patterns-procurement.md and patterns-b2b-saas.md as scope-loaded. Additive and inert until an archetype module exists (H3 ships procurement): every scope currently loads the base library only, so behavior is unchanged, and a missing archetype module is skipped silently with no hard failure.
- Procurement archetype pattern module (modules/patterns-procurement.md): six schema-complete patterns (PR-01 Authenticated Gate Value Recovery, PR-02 Gate Route Disambiguation, PR-03 Procurement Model Explanation Layer, PR-04 Decision Data at the Commit Surface, PR-05 Commit-Surface Default-State Prompt, PR-06 Rep-Channel Self-Service Bridge), loaded additively by the archetype resolver when category.primary resolves to procurement. Measurement-maturity reframe across score.md, ice-scoring.md, and construct.md: the condition 'performance data present but conversions lack page attribution' caps revenue-attribution confidence only (no blanket Confidence cap), defaults the target metric to a variant-instrumented proxy, and emits a scored measurement-enablement prerequisite-experiment; Confidence is earned on proxy interpretability; construct.md Step 5b estimates feasibility on the proxy base rate instead of returning 'Cannot estimate' when a proxy is instrumentable. Context-derived hypotheses may exceed Confidence 3 only when they both cite named corroborating evidence and target a high-traffic, proxy-readable surface. Validator section [11] checks the procurement module exists with 6 patterns, and section [6] prohibited-content greps extended to cover it.
- Engagement-context reasoning input (engagement-constraints, optional): a soft input capturing delivery and governance state (release calendar, approval/governance bandwidth, measurement-infrastructure timeline, internal-tester/QA constraints, delivery-match risk). New detect.md Step 1d 'Engagement-Constraint Reasoning' is a reasoning procedure (five derivation patterns plus a generalization rule and two worked examples) that derives per-experiment timing, concurrency, tier, feasibility, and launch-gate implications from the input; it produces constraints, never hypotheses. score.md Step 7 gains a top-priority Layer 0 applying the derived constraints (release-window timing overrides ordering, concurrency ceiling, tier ceiling on high-risk surfaces) and renders the reasoning in the Sequencing Rationale prose. When the input is absent, Step 1d is skipped and sequencing falls back to LIFT plus dependencies, unchanged. The skill owns the reasoning; the facts come from the input. Legacy: optional .claude/context/engagement-constraints.md; KB mode: maps to an engagement-context artifact. Also fixed a stale 'three layers' miscount in score.md Step 7.
- Output Format quality blocks and falsifiable magnitude: every hypothesis renders an 'Expected effect and read threshold' line (direction plus ship/abandon condition; MDE-based for proxy-only scopes, no fabricated point estimate), backed by a read-threshold requirement on the construct.md Step 4 Prediction. A behavioral-evidence block renders when a behavioral-friction signal (dead clicks, quickbacks, error rates) exists for the target surface, citing source and date. construct.md Step 9 gains a hard render check: a bundled hypothesis must carry the bundle-disclosure block with a non-empty 'This test will not isolate' line or it fails the render and must be revised before output.
- Detect triggers and untestable-differentiator routing: detect.md Step 1c gains two performance-driven triggers (a large non-search/non-self-referral source of unverified intent routes to a denominator/segmentation hypothesis or a client question; a cross-property referral with elevated quickback routes to a handoff-pathing hypothesis), plus a trigger-evaluation note to prefer routing a client question over inventing intent for an unknown population. detect-contextual.md quality-gate criterion 5 gains an untestable-differentiator routing branch: a highest-value differentiator that cannot be A/B tested (entitled-account-only, store-wide-claim-unsupportable, or otherwise structurally untestable at scope) routes to What's Not Here as a productization/positioning decision carrying its demand evidence and blocker, rather than being discarded or forced into a hypothesis; the criterion-gate prose was softened so the routing criteria (5 and 6) no longer read as a blanket discard. score.md Step 5b adds a named structurally-untestable-differentiator routing category distinct from low-traffic infeasibility.
- CTR-14 (Device/OS Defect Misattribution) added to the contrarian trigger matrix: when an OS-level or browser-family script-error defect explains a device friction gap, gate any device-targeted messaging hypothesis and route to fix-and-monitor (the fix is error remediation, not a messaging test or Core Web Vitals audit), capped at Strategic Bet tier until the error family is ruled out, because messaging tests on error-degraded sessions cannot separate message effect from error effect. The trigger-count line moved from 13 to 14, and validator section [12] now guards the contrarian-trigger count (14) and its count-line agreement, matching the pattern-count and procurement-count guards.
- KB read-side mapping: new optional silver-structural-observation row mapping to reference/cro-{scope}/live-structure.md, a KB-native structural projection with no legacy .claude/context/ equivalent, read only in KB mode; mode resolution is unchanged (the new type is optional, not a mode-resolution requirement). Phase A of the observation-artifact integration (chg in bound KB repo covers the artifact-type definition).
- Structural observation consumption (Phase C, chg_2026-06-09_hypothesis-generator-structural-consumption): detect.md gains a Step 1 structural extraction stanza (site-level and per-page fields, copy skeleton to generic matching, trust qualifiers downgrade to partial) and a new gated Step 1e field-keyed trigger table with mandatory tri-state semantics (present fires, absent suppresses or fires absence-conditioned patterns, not_checked does neither; a field missing from a page record is not_checked) and a structure-vs-behavior boundary rule (structure supplies targets and current-state confirmation, performance supplies firing counts for engagement patterns; no double-counting). construct.md gains a structural render check on mobile hypotheses (mobile_render_clean false routes fix-and-monitor, true rules out the rendering-defect family and the hypothesis proceeds as a layout/content test), a site-wide scope check in Step 1 (form_recurs_sitewide generalizes form hypotheses to the site-wide instance set; sitewide_primary_cta_label likewise for CTA label tests; observed element identifiers satisfy the specificity requirement for engagement targets), and observed current-state citation in Step 2 with an explicit no-baseline-from-structure rule. CTR-14 detection signals gain the mobile_render_clean false observation and its gate condition gains the true-clears-the-gate rule-out (extended in place, trigger count unchanged at 14). SKILL.md adds the KB-mode soft-input precondition with explicit no-confidence-penalty absence semantics, a /live-capture Prerequisites suggestion, a graceful-degradation row in detect.md, and Deliverable Purity additions (live-structure.md, structural observation references, raw observation field names). Out of scope by design: PE-01/02, NX-03, NX-05. Validator intentionally unchanged (no new files, patterns, or triggers).

### Fixed
- Validator (scripts/validate-hypothesis-generator.sh) realigned to the live skill: phase-file count 3 to 4, pattern count 28 to 32, ICE calibration checks anchored to section headings (a legitimate table-column mention of 'Confidence Calibration' was tripping the exact-count-1 assertion), and both validator section [6] prohibited-content greps (em-dash and external-tool reference) extended to cover modules/contrarian-triggers.md and modules/hypothesis-interactions.md. SKILL.md operating-mode reconciliation: removed the blanket 'no YAML frontmatter' rule that contradicted the KB-mode gold artifact contract (body-purity scoped to the body, the gold frontmatter block is the only system surface), added an Operating Modes section documenting legacy and KB modes, and labeled legacy-only Preconditions framing. No behavioral change to detection, construction, or scoring.
- Cross-surface bundling reconciliation: construct.md (Experiment Scope Rule) and hypothesis-interactions.md (Same-Page Interaction Analysis Process) previously disagreed on bundling across surfaces, which permitted an uninterpretable three-surface bundle. Both files now carry the same rule verbatim with reciprocal keep-in-sync cross-references: bundle elements that serve one idea on the same page or section; across surfaces bundle only if a single failure mode can be named for a combined loss; if a combined loss would list three or more independent failures across surfaces, sequence instead of bundling.
- Module resolution hardening: Phase 1 now states that modules/<name>.md references are repository-root-relative (modules/ is a sibling of skills/, not inside the skill dir) with symlink-aware resolution for isolated or installed invocations (e.g., ~/.claude/skills/), plus a hard load-failure guard: if the pattern library, ICE calibration, or contrarian/interaction modules cannot be read, STOP and report rather than silently falling back to embedded CRO judgment. Fixes a latent failure where a symlinked invocation resolved modules/ to the empty skill dir and produced a plausible-but-unbacked roadmap. Module Dependencies notes the convention.

### Changed
- Self-critique is now rendered on every hypothesis regardless of tier (Output Format template, Quality Rule 17, construct.md Step 10), reversing the prior allowance for Exploration-tier hypotheses to omit the rendered section. The self-critique meta-pass already ran unconditionally; this makes the rendered section universal.
