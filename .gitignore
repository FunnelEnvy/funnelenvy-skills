# Environment and credentials
.env
.env.*
!.env.example
*.pem
*.key
*credentials*.json
*service-account*.json

# Development directories (may contain client references and sensitive data).
# NEVER commit change docs / _dev to this PUBLIC repo. Matches root _dev/ and any nested _dev/.
_dev/
skills/**/_dev/

# Module bundles accidentally written into a skill dir (shared modules live at repo-root modules/,
# a sibling of skills/; a symlinked/isolated invocation can deposit a stale copy here -- see ARCHITECTURE.md gotcha 16)
skills/**/modules/

# Internal architecture reference (header: "Not shipped in the public repo")
/ARCHITECTURE.md

# Execution plans (development artifacts, not source)
execution-plan/

# Test run outputs (generated, not source)
test-runs/

# Claude Code context files (generated per-project)
.claude/context/
.claude/deliverables/
.claude/settings.local.json

# Business briefs contain client-specific data
.claude/business-brief.md

# Internal content and session state
content-briefs/
start.md

# Task tracking (session-specific)
tasks/
TODO/

# Python bytecode
__pycache__/

# OS artifacts
.DS_Store
Thumbs.db

# Editor files
*.swp
*.swo
*~
.vscode/
.idea/
